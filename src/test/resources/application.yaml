spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
    initialization-mode: always
    platform: h2
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false